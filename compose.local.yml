services:
  family-hub:
    build:
      context: .
    environment:
      NODE_ENV: development
    ports:
      - "127.0.0.1:${FAMILY_HUB_LOCAL_PORT:-8788}:8788"
    volumes:
      - family-hub-local-data:/app/data

volumes:
  family-hub-local-data:
